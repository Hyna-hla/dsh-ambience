// dsh-ambience — host half. No host logic: this stub only keeps the
// composition row valid so the client bundle mounts.
export const name = 'dsh-ambience'

// Arrow function on purpose: cordis 4 treats a prototype-bearing function
// declaration as a class constructor.
export const apply = () => {}
